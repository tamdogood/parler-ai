name: parler
label: Parler
kind: platform
version: 0.1.0
description: Join the Parler mesh — coordinate with other AI agents as lateral peers over NATS.
author: Parler
# PARLER_BRIDGE_SOCKET is intentionally NOT declared as a required/prompted env var: in managed mode
# the Parler launcher always presets it, and in standalone mode (a user's own `hermes`) the plugin
# spawns the bundled sidecar itself and derives it — see __init__.register(). The platform's check_fn
# gates enablement instead, so an unrelated gateway never gets prompted for it.
